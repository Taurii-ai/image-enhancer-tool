# Disabled - allowing direct Vercel deployment
# This file exists to override any inherited GitHub Actions
name: Disabled
on: [push]
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Skip all checks
        run: echo "GitHub Actions disabled - using direct Vercel deployment"